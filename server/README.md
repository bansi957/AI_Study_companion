# Server

Backend scaffold for the AI Study Companion app.

## Scripts

- `npm start` - run the server
- `npm run dev` - run with nodemon
- `npm test` - run the Node test suite

## Environment

Copy `.env.example` to `.env` and update the values before running the app.
